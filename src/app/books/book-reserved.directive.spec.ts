import { BookReservedDirective } from './book-reserved.directive';

describe('BookReservedDirective', () => {
  it('should create an instance', () => {
    const directive = new BookReservedDirective();
    expect(directive).toBeTruthy();
  });
});
